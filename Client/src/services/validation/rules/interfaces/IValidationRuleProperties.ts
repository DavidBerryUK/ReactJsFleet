import IValidationRule from "./IValidationRule";

// element properties for the validation rules, it is just a list of rules
//
export interface IValidationRuleProperties {
    rules : Array<IValidationRule>;
}